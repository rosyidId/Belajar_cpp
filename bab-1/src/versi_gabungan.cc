/******************************************************
Nama file: versi_gabungan.cpp
*******************************************************/

#include <iostream>
#include <cstdio>

using namespace std;

int main() 
{

  int angka;

  // menampilkan teks untuk informasi
  cout << "Masukkan bilangan bulat: "; 

  // membaca nilai dari keyboard 
  // dan menyimpannya ke dalam variabel angka
  cin >> angka;

    /* menampilkan nilai yang telah dimasukkan */
  printf("Bilangan yang dimasukkan adalah %d \n", angka);

  return 0;
}
